name: authentication_repository
description: Authentication repository.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  cache:
    path: ../cache
  equatable: ^2.0.5
  supabase: ^1.11.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0